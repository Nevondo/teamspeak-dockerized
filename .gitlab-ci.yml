image: docker:latest

services:
  - docker:dind


build-master:
  stage: build
  script:
    - docker login git.codeink.de:4567 -u gitlab+deploy-token-1 -p y_7Xej84e8tyq5zipgMw
    - docker build -t git.codeink.de:4567/codeink/teamspeak-dockerized .
    - docker push git.codeink.de:4567/codeink/teamspeak-dockerized
  only:
    - master
